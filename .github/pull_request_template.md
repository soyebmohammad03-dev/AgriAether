## Summary

What does this PR change, and why?

## Tests

- [ ] `npm run typecheck` passes
- [ ] `npm run test` passes
- [ ] `npm run build` passes
- [ ] (if `ml/` touched) relevant Python script re-run and manifests updated

## Documentation

- [ ] README / `ml/README.md` updated if behavior, metrics, or claims changed

## Screenshots

If this changes the UI, attach a before/after screenshot.

## Data / model / provenance impact

Does this PR touch an `Observation`, `PredictionRecord`, `ModelRecord`,
dataset, or ML metric? If so, describe the impact — if not, write "None."
